[tool.poetry]
name = "setup"
version = "0.1.0"
description = ""
authors = ["Rick Kilcoyne <rkilcoyne@cloudbolt.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

